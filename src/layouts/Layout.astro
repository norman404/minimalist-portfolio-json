---
import { basics } from '@cv'
interface Props {
	title: string;
}

const { title } = Astro.props;
const { image, summary } = basics
---

<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<title>{title}</title>
		<meta name="description" content={summary} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="preload" as="image" href={image} />
	</head>
	<body>
    <div class="languages">
      <a href='/'>English</a>
      <a href='/es'>Spanish</a>
    </div>
		<slot />
	</body>
</html>

<style is:global>
	html {
    font-family:
      Menlo,
      Monaco,
      Lucida Console,
      "Courier New",
      Courier,
      monospace;
    background: #fff;
    letter-spacing: -0.025rem;
  }
  body,figure {
    margin: 0;
    padding: 0;
  }
  a {
    text-decoration: none;
  }

  ul {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  *, *::before, *::after {
    box-sizing: border-box;
  }

  h1, h2, h3, h4 {
    margin: 0;
    font-family:
      system-ui,
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      Roboto,
      Oxygen,
      Ubuntu,
      Cantarell,
      "Open Sans",
      "Helvetica Neue",
      sans-serif;
  }

  p {
    color: #666;
    font-size: 0.9rem;
    line-height: 1.5;
    margin: 0;
    text-wrap: pretty;
  }

  .print {
    display: none !important;
  }

  @media print {
    .no-print {
      display: none !important;
    }

    .print {
      display: block !important;
    }

    astro-dev-toolbar {
      display: none !important;
    }

    .languages {
      display: none !important;
    }

    @page {
      margin: 1cm;
    }

    article {
      break-inside: avoid;
    }

    /* Compacta el layout para que entre en una sola página al imprimir */
    main {
      padding: 0 !important;
    }

    section {
      margin: 0 auto 9px !important;
    }

    section h2 {
      font-size: 1rem !important;
      margin-bottom: 2px !important;
    }

    ul {
      gap: 9px !important;
    }

    p {
      font-size: 0.72rem !important;
      line-height: 1.25 !important;
    }

    h1 {
      font-size: 1.4rem !important;
    }

    /* Hero más compacto */
    .info {
      gap: 0.25rem !important;
    }

    .info h2 {
      font-size: 0.95rem !important;
    }

    .info span {
      font-size: 0.75rem !important;
    }

    /* Encabezados de cada experiencia/educación más juntos */
    header {
      margin-bottom: 2px !important;
    }
  }
  /* Esto hace que el contenedor de "languages" sea un flex container */
.languages {
  display: flex; /* Activa Flexbox */
  justify-content: center; /* Centra los elementos horizontalmente */
  align-items: center; /* Centra los elementos verticalmente */
  gap: 10px; /* Espacio entre los elementos "a" */
}

/* Estilos adicionales para los enlaces, para que sean más discretos */
.languages a {
  text-decoration: none; /* Elimina el subrayado */
  color: #666; /* Color discreto */
  font-size: 0.8rem; /* Tamaño de fuente más pequeño */
}

</style>
